(() => {
  const key='ipasExplorer';
  const read=()=>{try{return JSON.parse(localStorage.getItem(key)||'null')}catch{return null}};
  let joining=null;
  function status(message){let node=document.getElementById('syncStatus');if(!node){node=document.createElement('p');node.id='syncStatus';node.setAttribute('role','status');node.className='sync-status';document.body.append(node)}node.textContent=message}
  async function api(path,body,method){const identity=read();const response=await fetch('./api/'+path,{method:method||(body?'POST':'GET'),signal:AbortSignal.timeout(8000),headers:{'Content-Type':'application/json',...(identity?{Authorization:'Bearer '+identity.token}:{})},...(body?{body:JSON.stringify(body)}:{})});if(!response.ok)throw Error(response.status===401?'匿名憑證已失效；請刪除失效身分後重新加入。':response.status===429?'同步太頻繁，請稍後再試；本機仍可玩。':'世界榜尚未連線或免費容量暫時用盡；個人練習仍可使用。');const type=response.headers.get('content-type')||'';if(!type.includes('application/json'))throw Error('世界榜後端尚未啟用；你的個人紀錄仍在此裝置。');return response.json();}
  window.Community={read,async join(){if(read())return read();if(joining)return joining;joining=(async()=>{const i=await api('join',{});localStorage.setItem(key,JSON.stringify(i));return i})();try{return await joining}finally{joining=null}},rank:level=>api('ranking?level='+level),async answer(level,target,choice){if(!read()){status('本機練習 · 尚未加入世界榜');return false}status('世界榜同步中…');try{const r=await api('answer',{level,target,choice});if(r.saved!==true)throw Error();status(r.added?'世界榜已確認 · 新知識點 +1 ★':'世界榜已確認 · 此知識點已計星');return true}catch{status('本題未確認同步 · 本機紀錄保留，連線恢復後可再答此題');return false}},async remove(){if(read())await api('me',null,'DELETE');localStorage.removeItem(key);status('雲端身分已刪除，本機學習紀錄保留')}};
})();
